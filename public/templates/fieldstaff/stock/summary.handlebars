{{#when item}}
<table>
    <thead>
        <tr>
            <th>Product</th>
            <th>Available</th>
        </tr>
    </thead>
    <tbody>
        {{#each item}}
        <tr>
            <td><a href="#!f/product/{{productId}}">{{productName}}</a></td>
            <td>{{available}}</td>
        </tr>
        {{/each}}
     </tbody>
</table>
{{else}}
No stock availability information available.
{{/when}}

