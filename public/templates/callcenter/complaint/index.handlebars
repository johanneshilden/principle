{{#when complaint}}
<table>
    <thead>
        <tr>
            <th>Id</th>
            <th>Title</th>
            <th>Customer</th>
        </tr>
    </thead>
    <tbody>
        {{#each complaint}}
        <tr>
            <td>{{id}}</td>
            <td><a href="#!c/complaint/{{id}}">{{title}}</a></td>
            <td>{{#if resolved}}{{resolved}}{{else}}Unresolved{{/if}}</td>
            <td>{{customer}}</td>
        </tr>
        {{/each}}
     </tbody>
</table>
{{else}}
No complaints.
{{/when}}
